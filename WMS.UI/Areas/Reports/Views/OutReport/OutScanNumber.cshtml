@model WMS.Report.Controllers.Model.OutScanNumberModel
@{
    Layout = "../../../../Views/head.cshtml";
}
<div id=layout>
    <div form="Y" id="form1">
        <input type="hidden" name="whCode" value="@ViewBag.whCode" />
        @Html.EipGetSelect((IEnumerable<SelectListItem>)ViewData["clientCode"], "WhClientId", "客户名", "", "")
        <input type="text" left="100" name="LoadId" hint="Load号" />
      
        <input type="text" name="PoNumber" hint="PO" />
        <input type="text" name="ALtItemNumber" hint="SKU" />
        <input type="text" name="HuId" hint="托盘" />

        @Html.EipInputFor(model => model.OutDateBegin, new { @left = "100", @value = DateTime.Now.AddDays(0).ToString("yyyy-MM-dd") })
        @Html.EipInputFor(model => model.OutDateEnd, new { @left = "20", @value = DateTime.Now.ToString("yyyy-MM-dd") })
        <textarea name="SoNumber" left="5" hint="SO"></textarea>
        <textarea name="ScanNumber" left="5" hint="序列号" ></textarea>
    </div>
    <table id="grid"></table>
</div>

<script type="text/javascript" src="/share/includes/eip.js"></script>
<script type="text/javascript">

    $(function () {

        //页面布局
        var layout = eip.layout.create(
            $("#layout"),
            [{
                obj: $('#form1'),

                buttons: [
                    {
                        text: "查询", icon: "search", click: function () {
                            eip.datagrid.load("/Reports/OutReport/OutScanNumberList", $("#grid"), eip.para.create($("input,select,textarea", $("#form1"))));
                        }
                    }
                ]
            }]);

        //客制化
        eip.datagrid.create({
            obj: $("#grid"),
            pagesize: 50,
            ajaxType: 'post',    //load时，ajax请求类型
            sort: true,
            // sorts: { "ReceiptDate": "DESC", "托盘号": "ASC" },  //排序栏位：{'料号':'DESC','组织':'ASC'}
            showExcelExport: true,//显示汇出按钮
            //  hiddencolumns: ["ReceiptDate"],
            //showOrderBtns: true,
            //lockcolumns: 2,
            columns: {
                "操作": {
                    formatter: function (row) {
                        var retHtml = " ";

                        return retHtml;
                    }
                },
                dofunc: function (grid, trs) {
                    //for (i in trs)  //列 格式化增加颜色显示
                    //    $("td[field=UserNameCN],td[field=PassWord]", trs[i]).css('color', 'blue');
                }
            }
        });


        $("[name=SoNumber]").css("height", "45px").css("width", "185px");
        $("[name=SoNumber]").parent().css("height", "50px").css("width", "240px");

        $("[name=ScanNumber]").css("height", "45px").css("width", "185px");
        $("[name=ScanNumber]").parent().css("height", "50px").css("width", "240px");
    });

</script>